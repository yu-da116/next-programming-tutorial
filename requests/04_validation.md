# TASK-04: バリデーションの強化（Zod）

## 概要
入力バリデーションを API とフロントでさらに強化してください。

## 要件
- [ ] `name` の前後空白を除去して検証する。
- [ ] `price`, `stock` は整数のみ許可する。
- [ ] エラー形式を `{ "message": "..." }` に統一する。
- [ ] バリデーション定義を 1 箇所に集約する。

## ヒント
- `z.string().trim()` や `z.number().int()` が使えます。


