# TASK-05: ページネーションの実装

## 概要
商品一覧にページネーションを追加してください。

## 要件
- [ ] API (`GET /api/products`) が `page`, `pageSize` を受け取る。
- [ ] `items` に加えて `total`, `page`, `pageSize` を返す。
- [ ] 一覧画面に「前へ/次へ」ボタンを追加する。
- [ ] ページ番号をURLクエリに反映する。

## ヒント
- Prisma の `skip` / `take` を使用します。


