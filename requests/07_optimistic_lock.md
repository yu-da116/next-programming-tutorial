# TASK-07: 楽観的ロック（Optimistic Locking）の導入

## 概要
同時編集時の上書き事故を防ぐため、楽観的ロックを導入してください。

## 要件
- [ ] 更新時に `updatedAt` を比較して競合を検出する。
- [ ] 競合時は 409 を返す。
- [ ] フロントで「他ユーザーに更新されました」を表示する。
- [ ] 正常更新時は従来通り `/products` へ戻す。

## ヒント
- `where: { id, updatedAt }` で更新し、0件更新を競合扱いにできます。


