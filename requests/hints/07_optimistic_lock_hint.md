# TASK-07 ヒント: 楽観的ロック

## 実装方針
- 編集画面読み込み時に `updatedAt` を保持します。
- 更新時に API へ `updatedAt` も送信し、条件一致時のみ更新します。

## Prismaの考え方
- `updateMany` + `where: { id, updatedAt }` で競合判定しやすくなります。
- `count === 0` なら 409 を返します。

## UI側
- 409 受信時に「他ユーザーに更新されました。再読み込みしてください。」を表示します。
