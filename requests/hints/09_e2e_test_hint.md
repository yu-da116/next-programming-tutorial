# TASK-09 ヒント: E2Eテスト

## 最初の一歩
- Playwright を導入し、最初は `/products` の表示確認テストから始めます。
- 次に「作成→一覧反映」「削除→一覧反映」を追加します。

## 安定化のコツ
- テスト前にDBを初期化する手順を固定化します。
- セレクタはテキスト依存を減らし、`data-testid` 付与も検討します。

## CI向け
- `docker compose up -d db web` の待機を入れてから実行します。
